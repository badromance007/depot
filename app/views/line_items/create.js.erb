$('#notice').hide();

if ($('#cart tr').length == 1) { $('#cart').show('blind', 1000);}

$('#cart').html("<%= escape_javascript render(@cart) %>");

$('#current_item').css({'color':'#FFFF00', 'font-weight':'bold'}).animate({'color':'#fff', 'font-weight':'normal'}, 1000);